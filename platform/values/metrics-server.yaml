# metrics-server — mínimo para t3.small
args:
  - --kubelet-insecure-tls
  - --kubelet-preferred-address-types=InternalIP

resources:
  requests:
    cpu: 25m
    memory: 48Mi
  limits:
    cpu: 50m
    memory: 96Mi
